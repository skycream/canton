// Canton JWT Authentication Configuration
// Supports JWKS-based token verification (Keycloak, Auth0, etc.)

_auth_services = [
  {
    type = jwt-rs-256-jwks
    url = "http://localhost:8080/realms/canton/protocol/openid-connect/certs"
    url = ${?CANTON_AUTH_JWKS_URL}
  }
]
